import { FC } from 'react'

import { Authorization } from '../components/layout/screens/authorization/Authorization'

export const AuthorizationPage: FC = () => {
  return (
    <Authorization />
  )
}